{
  "name": "offline-helper-payments",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for Stripe Checkout Sessions and webhooks on offlinehelpers.com",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
