{
  "name": "offline-helper-payments",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker for Stripe Checkout Sessions and webhooks on offlinehelpers.com",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node tests/webhook-fulfillment-alert.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
